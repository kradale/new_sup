# Руководство разработчика - ElemApiWidget

## 🏗️ Архитектура виджета

### Структура компонентов

```
ElemApiWidget/
│
├── ElemApiWidget.vue              # Основной компонент (точка входа)
│   ├── JsonTreeView.vue          # Компонент древовидного отображения
│   └── JsonTableView.vue         # Компонент табличного отображения
│
├── descriptor.js                  # Дескриптор виджета (props, metadata)
├── config.js                      # Конфигурация опций
├── constants.js                   # Константы приложения
├── utils.js                       # Утилиты и хелперы
│
├── panels/                        # Панели настроек
│   ├── ApiConfigPanel.vue        # Настройка API параметров
│   ├── DisplayPanel.vue          # Настройка отображения
│   ├── StylePanel.vue            # Настройка стилей
│   ├── DatasetPanelMixin.js      # Общий миксин панелей
│   └── index.js                  # Экспорт панелей
│
├── types/                         # TypeScript типы
│   ├── index.d.ts                # Определения типов
│   └── index.js                  # Экспорт типов
│
├── style.pcss                     # Стили виджета
├── README.md                      # Пользовательская документация
├── DEVELOPER_GUIDE.md            # Документация для разработчиков
└── examples.json                  # Примеры конфигураций
```

## 🔧 Основные компоненты

### 1. ElemApiWidget.vue

Главный компонент виджета, отвечающий за:
- Выполнение HTTP запросов
- Управление состоянием (loading, success, error)
- Отображение результатов
- Взаимодействие с пользователем

**Ключевые методы:**
- `handleExecuteRequest()` - выполнение API запроса
- `handleCopyResponse()` - копирование ответа в буфер
- `handleDownloadResponse()` - скачивание JSON файла
- `setupAutoRequest()` - настройка автоматических запросов

**Computed свойства:**
- `displayData` - данные для отображения с учетом `responsePath`
- `colorScheme` - объект с цветовой схемой
- `containerStyles` - CSS переменные для стилизации

### 2. JsonTreeView.vue

Рекурсивный компонент для отображения JSON в виде дерева.

**Особенности:**
- Поддержка сворачивания/разворачивания узлов
- Ограничение глубины вложенности
- Цветовая индикация типов данных
- Рекурсивный рендеринг

**Props:**
- `data` - данные для отображения
- `maxDepth` - максимальная глубина
- `currentDepth` - текущий уровень вложенности
- `colors` - цветовая схема

### 3. JsonTableView.vue

Компонент для табличного отображения данных.

**Особенности:**
- Автоматическое определение колонок
- Форматирование значений по типам
- Адаптивная таблица

**Props:**
- `data` - данные (объект или массив)
- `colors` - цветовая схема

## 🎨 Система стилизации

### CSS переменные

Виджет использует CSS переменные для гибкой настройки:

```css
--primary-color: #3b82f6;
--success-color: #10b981;
--error-color: #ef4444;
--font-size: 14px;
--border-radius: 8px;
--padding: 16px;
```

### Классы и модификаторы

**Кнопки:**
- `.api-widget-btn` - базовый класс кнопки
- `.api-widget-btn-primary` - основная кнопка
- `.api-widget-btn-secondary` - второстепенная кнопка
- `.api-widget-btn-loading` - состояние загрузки

**Статусы:**
- `.status-success` - успешный статус (2xx)
- `.status-error` - ошибка (4xx, 5xx)
- `.status-warning` - предупреждение (3xx)

## 🔌 Панели настроек

### ApiConfigPanel.vue

Панель для настройки параметров API запроса.

**Компоненты UI:**
- `ui-input` - текстовое поле
- `ui-select` - выпадающий список
- `ui-textarea` - многострочное поле
- `ui-checkbox` - чекбокс

**Валидация:**
- URL валидируется на наличие значения
- Headers парсятся построчно
- Body валидируется по формату

### DisplayPanel.vue

Панель настроек отображения результатов.

**Опции:**
- Режим отображения (tree/table/raw)
- Глубина JSON (1-5 уровней или все)
- Флаги отображения дополнительной информации

### StylePanel.vue

Панель настройки визуального оформления.

**Настройки:**
- Цветовая схема (ui-color-picker)
- Размеры шрифта
- Границы и отступы

## 🛠️ Утилиты (utils.js)

### parseHeaders(headersString)
Парсит строку заголовков в объект.

```javascript
const headers = parseHeaders("Content-Type: application/json\nAuthorization: Bearer token");
// { "Content-Type": "application/json", "Authorization": "Bearer token" }
```

### parseBody(bodyString, format)
Парсит тело запроса в зависимости от формата.

```javascript
// JSON
parseBody('{"key": "value"}', 'json') // { key: "value" }

// Form Data
parseBody('key1=value1\nkey2=value2', 'formData') // FormData object

// Raw
parseBody('plain text', 'raw') // "plain text"
```

### getValueByPath(obj, path)
Извлекает значение из вложенного объекта по пути.

```javascript
const data = { user: { address: { city: "Moscow" } } };
getValueByPath(data, 'user.address.city') // "Moscow"
```

### limitDepth(obj, maxDepth, currentDepth)
Ограничивает глубину объекта.

```javascript
const deep = { a: { b: { c: { d: "value" } } } };
limitDepth(deep, 2) // { a: { b: '[...]' } }
```

### formatJson(data, indent)
Форматирует данные в JSON строку.

```javascript
formatJson({ key: "value" }, 2) // "{\n  \"key\": \"value\"\n}"
```

## 📊 Работа с данными

### Жизненный цикл запроса

1. **Инициализация** - пользователь настраивает параметры
2. **Подготовка** - парсинг headers и body
3. **Выполнение** - fetch запрос к API
4. **Обработка** - парсинг ответа (JSON/Text)
5. **Отображение** - визуализация результата

### Обработка ошибок

```javascript
try {
    const response = await fetch(url, options);
    if (!response.ok) {
        throw new Error(`HTTP ${response.status}`);
    }
    // обработка успешного ответа
} catch (error) {
    this.requestStatus = 'error';
    this.errorMessage = error.message;
}
```

## 🔄 Автообновление

### Настройка интервала

```javascript
setupAutoRequest() {
    if (this.intervalId) {
        clearInterval(this.intervalId);
    }
    
    if (this.props.requestInterval > 0) {
        this.intervalId = setInterval(() => {
            this.handleExecuteRequest();
        }, this.props.requestInterval * 1000);
    }
}
```

### Очистка при размонтировании

```javascript
beforeDestroy() {
    if (this.intervalId) {
        clearInterval(this.intervalId);
    }
}
```

## 🧪 Тестирование

### Рекомендуемые сценарии

1. **GET запрос без параметров**
   - URL: `https://jsonplaceholder.typicode.com/users`
   - Ожидаемый результат: массив пользователей

2. **POST запрос с JSON**
   - URL: `https://jsonplaceholder.typicode.com/posts`
   - Body: `{"title": "test", "body": "content", "userId": 1}`
   - Ожидаемый результат: созданный объект с id

3. **Обработка ошибок**
   - URL: `https://jsonplaceholder.typicode.com/invalid`
   - Ожидаемый результат: отображение ошибки 404

4. **Работа с путем к данным**
   - URL: `https://jsonplaceholder.typicode.com/users/1`
   - Path: `address.geo`
   - Ожидаемый результат: объект с координатами

## 🔐 Безопасность

### CORS
Виджет работает в контексте браузера и подчиняется политике CORS. Убедитесь, что целевой API разрешает запросы с вашего домена.

### Аутентификация
- Используйте заголовки для передачи токенов
- Не храните чувствительные данные в props
- Рекомендуется использовать переменные окружения

### Валидация
- Всегда валидируйте входящие данные
- Проверяйте Content-Type ответа
- Обрабатывайте ошибки парсинга

## 🚀 Оптимизация

### Производительность

1. **Ленивая загрузка компонентов**
   ```javascript
   components: {
       JsonTreeView: () => import('./components/JsonTreeView.vue')
   }
   ```

2. **Ограничение глубины JSON**
   - Используйте `maxDepth` для больших объектов
   - Отключайте автораскрытие для больших структур

3. **Дебаунсинг автозапросов**
   - Не устанавливайте слишком малые интервалы
   - Очищайте интервалы при размонтировании

## 🐛 Отладка

### Логирование

Для отладки добавлены console.log в ключевых местах:

```javascript
console.log('API Request:', { url, method, headers, body });
console.error('API Error:', error);
```

### DevTools

Используйте Vue DevTools для:
- Просмотра состояния компонента
- Отслеживания props
- Анализа performance

### Network Tab

В браузере отслеживайте:
- Статус запросов
- Заголовки
- Тело запроса/ответа
- Время выполнения

## 📝 Расширение функционала

### Добавление нового метода HTTP

1. Добавьте в `constants.js`:
   ```javascript
   export const HTTP_METHODS = {
       // ...
       PATCH: 'PATCH'
   };
   ```

2. Обновите `config.js`:
   ```javascript
   export const HTTP_METHOD_OPTIONS = [
       // ...
       { label: 'PATCH', value: 'PATCH' }
   ];
   ```

### Добавление нового режима отображения

1. Создайте компонент в `components/`
2. Импортируйте в `ElemApiWidget.vue`
3. Добавьте в `v-if` цепочку отображения
4. Обновите `config.js` с новой опцией

## 🔗 Интеграция с платформой

### Регистрация виджета

В `widgets.local.json`:
```json
{
  "elements": [
    {
      "type": "vendor::ElemApiWidget",
      "name": "API виджет",
      "icon": "mdi-api"
    }
  ]
}
```

### Dataset интеграция

Виджет использует `useElemDatasetMixin` для интеграции с системой данных платформы:

```javascript
const DatasetMixin = useElemDatasetMixin({
    drilldown: false,
    panel: false,
});
```

## 📚 Дополнительные ресурсы

- [Vue.js документация](https://v2.vuejs.org/)
- [Fetch API](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API)
- [JSON](https://www.json.org/)
- [REST API Best Practices](https://restfulapi.net/)

## 🤝 Вклад в развитие

При внесении изменений:
1. Следуйте существующему code style
2. Добавляйте JSDoc комментарии
3. Обновляйте документацию
4. Тестируйте изменения
5. Проверяйте линтером (ESLint)

---

**Версия:** 1.0.0  
**Последнее обновление:** 2025-10-10


