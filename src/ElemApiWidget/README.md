# ElemApiWidget - API Виджет

Универсальный виджет для работы с REST API. Позволяет выполнять HTTP запросы, настраивать параметры, заголовки и отображать результаты в различных форматах.

## Возможности

### 🔌 HTTP Запросы
- Поддержка всех основных HTTP методов: **GET**, **POST**, **PUT**, **DELETE**
- Настройка произвольных заголовков (Headers)
- Передача данных в теле запроса в разных форматах:
  - **JSON** - структурированные данные
  - **Form Data** - данные форм
  - **Raw Text** - произвольный текст
  - **None** - без тела (для GET запросов)

### 📊 Визуализация данных
Три режима отображения JSON ответа:
1. **Дерево (Tree)** - иерархическое отображение с возможностью сворачивания/разворачивания узлов
2. **Таблица (Table)** - табличный вид для массивов объектов
3. **Сырой JSON (Raw)** - форматированный JSON код

### ⚙️ Настройки отображения
- **Глубина вложенности** - контроль отображаемых уровней JSON (1-5 или все)
- **Путь к данным** - извлечение конкретной части из ответа (например, `data.items`)
- Отображение/скрытие:
  - HTTP статус кода
  - Времени ответа
  - Заголовков ответа

### 🎨 Стилизация
Гибкие настройки визуала:
- **Цветовая схема**:
  - Основной цвет
  - Цвет успеха (для успешных запросов)
  - Цвет ошибки (для ошибок)
- **Размеры**:
  - Размер шрифта (12-20px)
  - Радиус границ
  - Внутренние отступы

### 🔄 Автоматизация
- **Автозапрос** - автоматическое выполнение при загрузке виджета
- **Интервал запросов** - периодическое обновление данных (в секундах)

### 💾 Экспорт данных
- **Копирование** - копирование JSON в буфер обмена
- **Скачивание** - сохранение ответа в файл JSON

## Панели настроек

### 1. API Конфигурация
Основные параметры запроса:
- URL адрес API
- HTTP метод
- Заголовки (каждый на новой строке в формате `Ключ: Значение`)
- Формат и содержимое тела запроса
- Путь к данным в ответе
- Настройки автозапроса

### 2. Отображение
Настройки визуализации:
- Режим отображения (Дерево/Таблица/JSON)
- Глубина JSON структуры
- Чекбоксы для отображения дополнительной информации

### 3. Стилизация
Настройка внешнего вида:
- Цветовая палитра
- Размеры шрифта
- Границы и отступы

## Примеры использования

### Пример 1: Простой GET запрос

**URL:** `https://api.example.com/users`  
**Метод:** GET  
**Заголовки:**
```
Content-Type: application/json
Authorization: Bearer your-token-here
```

### Пример 2: POST запрос с JSON данными

**URL:** `https://api.example.com/users`  
**Метод:** POST  
**Формат Body:** JSON  
**Body:**
```json
{
  "name": "Иван Иванов",
  "email": "ivan@example.com",
  "age": 30
}
```

### Пример 3: Работа с вложенными данными

Если API возвращает:
```json
{
  "status": "success",
  "data": {
    "users": [
      { "id": 1, "name": "User 1" },
      { "id": 2, "name": "User 2" }
    ]
  }
}
```

**Путь к данным:** `data.users`  
Виджет отобразит только массив пользователей.

### Пример 4: Form Data

**URL:** `https://api.example.com/upload`  
**Метод:** POST  
**Формат Body:** Form Data  
**Body:**
```
username=admin
password=secret
remember=true
```

## Технические детали

### Структура файлов
```
ElemApiWidget/
├── ElemApiWidget.vue          # Главный компонент
├── descriptor.js              # Описание props виджета
├── config.js                  # Конфигурация опций
├── constants.js               # Константы
├── utils.js                   # Утилиты
├── style.pcss                 # Стили
├── components/
│   ├── JsonTreeView.vue      # Компонент дерева JSON
│   └── JsonTableView.vue     # Компонент таблицы
├── panels/
│   ├── ApiConfigPanel.vue    # Панель настройки API
│   ├── DisplayPanel.vue      # Панель отображения
│   ├── StylePanel.vue        # Панель стилизации
│   ├── DatasetPanelMixin.js  # Миксин
│   └── index.js              # Экспорт панелей
└── types/
    ├── index.d.ts            # TypeScript типы
    └── index.js              # Экспорт типов
```

### Props виджета

| Prop | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `apiUrl` | String | '' | URL адрес API |
| `httpMethod` | String | 'GET' | HTTP метод |
| `headers` | String | '' | Заголовки запроса |
| `bodyFormat` | String | 'none' | Формат тела запроса |
| `bodyContent` | String | '' | Содержимое тела |
| `responsePath` | String | '' | Путь к данным в JSON |
| `displayMode` | String | 'tree' | Режим отображения |
| `maxDepth` | Number | 3 | Глубина JSON |
| `autoRequest` | Boolean | false | Автозапрос |
| `requestInterval` | Number | 0 | Интервал в секундах |
| `primaryColor` | String | '#3b82f6' | Основной цвет |
| `successColor` | String | '#10b981' | Цвет успеха |
| `errorColor` | String | '#ef4444' | Цвет ошибки |
| `fontSize` | String | '14px' | Размер шрифта |
| `borderRadius` | String | '8px' | Радиус границ |
| `padding` | String | '16px' | Отступы |
| `showHeaders` | Boolean | true | Показывать заголовки |
| `showStatusCode` | Boolean | true | Показывать статус |
| `showResponseTime` | Boolean | true | Показывать время |
| `enableCopy` | Boolean | true | Копирование |
| `enableDownload` | Boolean | true | Скачивание |

## Accessibility (A11y)

Виджет поддерживает базовые возможности доступности:
- Кнопки с `aria-label`
- Поддержка навигации с клавиатуры (`tabindex`)
- Семантическая разметка HTML

## Совместимость

- **Vue.js**: 2.7.x
- **goodt-wcore**: 1.14.x
- **Браузеры**: Все современные браузеры с поддержкой ES6+

## Лицензия

ISC


